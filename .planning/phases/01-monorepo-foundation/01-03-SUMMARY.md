# Plan 3 Summary: Go Workspace, .gitignore, Wails Bindings

**Date:** 2026-04-15
**Branch:** worktree-agent-a9bd7e40 (targeting FG-5091)
**Commit:** 98326ae

## Tasks Completed

### Task 3-1: go.work Created
- Written to repo root with `go 1.23` and `use (./cli ./ui)`
- `go work sync` completed successfully — downloaded 3 new dependencies
- `go env GOWORK` correctly resolves to the repo root path
- `go.work.sum` was generated and committed

### Task 3-2: Unified .gitignore Written
- Replaced old .gitignore with unified version covering OS, editor, Go artifacts, CLI runtime artifacts, Wails build output, frontend deps, secrets, and logs
- Key note: `go.work` and `go.work.sum` are explicitly NOT ignored
- CLI runtime artifact pattern fixed to `cli/**/safetyculture-exporter*.yaml` (glob needed for nested paths)
- `ui/frontend/dist/.gitkeep` force-tracked with `git add -f`

### Task 3-3: Wails CLI v2.12.0 Installed; Bindings Regenerated
- Wails v2.12.0 installed via `go install github.com/wailsapp/wails/v2/cmd/wails@v2.12.0`
- `wails version` confirmed v2.12.0
- **Fix applied:** `wailsjsdir` in `ui/wails.json` corrected from `./frontend/src/wailsjs` to `./frontend/src` — Wails appends `wailsjs/` itself, so the old value produced a nested `wailsjs/wailsjs/` path
- `wails generate module` run from `ui/`; bindings generated at correct paths:
  - `ui/frontend/src/wailsjs/go/main/App.js`
  - `ui/frontend/src/wailsjs/go/main/App.d.ts`
  - `ui/frontend/src/wailsjs/go/models.ts`
  - `ui/frontend/src/wailsjs/runtime/runtime.js`
  - `ui/frontend/src/wailsjs/runtime/runtime.d.ts`
  - `ui/frontend/src/wailsjs/runtime/package.json`
- "Not found" log lines during generation are normal for anonymous struct types (Wails limitation)

### Task 3-4: Full Verification Suite

| Check | Result | Notes |
|-------|--------|-------|
| 1. `go env GOWORK` | PASS | Resolves to repo root go.work |
| 2. `cli/ go build ./...` | PASS | Compiler warnings from go-m1cpu (harmless) |
| 3. `cli/ go test ./...` | PASS | All 14 packages; 75s for pkg/api |
| 4. `ui/ go build ./...` | PASS | Workspace resolves cli/ locally |
| 5. `go work sync` | PASS | Clean after initial sync |
| 6. No `/cli` import corruption | PASS | 0 occurrences |
| 7. `GOWORK=off go build ./...` (cli) | PASS | Required `go mod tidy` first to fix go.sum |
| 8. No `replace` in ui/go.mod | PASS | Workspace supersedes replace directives |

**Note on check 7:** `GOWORK=off go build ./...` initially failed because `go work sync` had updated the workspace-level sums without updating `cli/go.sum`. Running `GOWORK=off go mod tidy` in `cli/` fixed this and updated several indirect dependency versions (crypto, sync, sys, text, etc. to newer patch releases).

### Task 3-5: Wails Build Smoke Test

**Result: FAIL (expected / documented)**

The `wails build` failed during frontend compilation:

```
Could not resolve '../../wailsjs/go/main/App.js' from src/routes/Welcome.svelte
```

**This is NOT a module/workspace resolution failure.** Root causes:
1. The Svelte source files import wailsjs bindings via relative paths that are now mismatched after the corrected `wailsjsdir`
2. The UI frontend is currently Svelte + Vite 3 — the legacy stack from the upstream UI repo, not the target React + Vite 5.4.x stack specified in CLAUDE.md

The Go workspace wiring is correct. `go build ./...` succeeds in both modules. The smoke test failure is a pre-existing frontend compatibility issue to be resolved during the frontend migration phase.

Failure documented in: `ui/build/smoke-test-failure.txt`

## Files Changed

| File | Action | Reason |
|------|--------|--------|
| `go.work` | Created | Workspace linking cli/ and ui/ |
| `go.work.sum` | Created | Generated by go work sync |
| `.gitignore` | Replaced | Unified rules for both modules |
| `cli/go.mod` | Modified | go work sync bumped indirect deps |
| `cli/go.sum` | Modified | go mod tidy after GOWORK=off fix |
| `ui/go.mod` | Modified | go work sync added kr/pretty indirect |
| `ui/go.sum` | Modified | Updated by sync |
| `ui/wails.json` | Modified | Corrected wailsjsdir path |
| `ui/frontend/dist/.gitkeep` | Created | Preserves dist/ for go:embed |
| `ui/frontend/pnpm-lock.yaml` | Created | pnpm install lockfile |
| `ui/frontend/src/wailsjs/*` | Created | Generated Wails JS bindings |
| `ui/build/smoke-test-failure.txt` | Created | Documents wails build failure |

## Decisions Made

- **wailsjsdir = `./frontend/src`** not `./frontend/src/wailsjs`: Wails appends `wailsjs/` to the configured dir. The CLAUDE.md reference to "wailsjsdir: ./frontend/src/wailsjs" describes the *output* location, not the config value.
- **go.work.sum committed**: Per .gitignore note, both `go.work` and `go.work.sum` are tracked — they are required for reproducible workspace builds.
- **cli/go.mod indirect dep bumps committed**: These are correct dependency upgrades triggered by workspace alignment with ui/'s dependency graph. Safe to commit.
